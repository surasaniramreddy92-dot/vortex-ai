# VORTEX desktop voice assistant dependencies
# NumPy 2.x is intentionally avoided because the ONNX wake-word stack currently
# uses the NumPy 1.x ABI.
numpy==1.26.4
psutil==7.2.2
pygame==2.6.1
pystray==0.19.5
sounddevice==0.5.5
SpeechRecognition==3.16.1
# Backend that SpeechRecognition's Microphone class requires.
PyAudio==0.2.14
Pillow==12.2.0
python-dotenv==1.2.2
edge-tts==7.2.8
ollama==0.6.2
openwakeword==0.6.0
onnxruntime==1.17.3
# Phase 7: document intelligence (PDF/DOCX/XLSX text extraction)
pymupdf==1.28.0
python-docx==1.2.0
openpyxl==3.1.5
# Phase 3: browser automation. After installing, also run:
#   python -m playwright install chromium
playwright==1.62.0
# Phase 5: RAG - requires PostgreSQL and Qdrant running locally (see README).
psycopg2-binary==2.9.12
qdrant-client==1.18.0
# Phase 5: sparse (BM25) half of hybrid dense+sparse retrieval, added
# 2026-08-16. Tiny, pure-Python, no dependency beyond numpy (already above) -
# deliberately NOT sentence-transformers, which would pull in torch (~122MB
# Windows wheel alone) for cross-encoder reranking; see rag.py's module
# docstring for the full reasoning.
rank_bm25==0.2.2