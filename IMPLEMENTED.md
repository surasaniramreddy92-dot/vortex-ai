# VORTEX — Implementation Matrix

Honest snapshot of what actually runs today. A phase is only marked
**Implemented** if its acceptance scenario runs end-to-end; if part of it
works it's **Partial**; **Started** means only groundwork exists. This file
only lists phases with real work — phases with no code yet aren't listed
here at all; they'll be added, one at a time, as each is actually built.

**This table tracks the master feature roadmap (Phase 0-16) — not the same
thing as `docs/REFACTOR_PLAN.md`'s Steps 0-10, an internal code-
organization refactor that finished 2026-08-18 (see Phase 0's row below).
That refactor changed how the code is structured; it didn't complete any
feature phase by itself, which is why most rows here are still "Partial"
even though the refactor itself is 100% done.**

Last updated: 2026-09-01.

| Phase | Name | Status | Notes |
|---|---|---|---|
| 0 | Engineering Foundation & Repository Discipline | **Partial** | Git repo, `.gitignore`, `CHANGELOG.md` (dated, day-by-day). Typed config (`src/vortex/config.py`, a frozen dataclass, every field env-driven with a documented default) landed via `docs/REFACTOR_PLAN.md` Steps 1-2. Step 3 (2026-08-16) extracted the voice subsystem — wake detection, TTS, STT, session/event handling — out of the `Vortex` god-object into `src/vortex/voice/` (`wake.py`, `tts.py`, `stt.py`, `audio.py`, `barge_in.py`, `session.py`), each a small class taking its dependencies as constructor arguments instead of reading module globals. Zero behavior change — verified via a full live acoustic test after merging (wake → capture → response → barge-in → distinct acknowledgment, same sub-100ms interrupt timing as before extraction) — plus 38 passing `pytest` tests, including `tools/test_barge_in.py`'s scenarios newly ported into `tests/unit/test_barge_in.py`. **The refactor resumed and finished 2026-08-18** (Steps 4-10, on hold since Step 3 while capability work below shipped instead): `src/vortex/main.py` shrank from the entire application to a 15-line bootstrap; the former god-object is now `app.py` (composition + document/RAG orchestration) plus `llm/` (LLM provider interface), `platform/` (OS-specific adapter, Windows implemented), `tools/system/` (OS-automation capability logic), and `core/` (`intent_router.py` — pure text→Intent classification, 26 types; `capability_registry.py` — dispatch; `policy_engine.py` — yes/no confirmation parsing; `orchestrator.py` — process lifecycle; `state_manager.py` — an explicit `VortexState` enum computed over the same events that already drove behavior, not a new source of truth). Two real, pre-existing security gaps got closed along the way rather than left open: the protected-process denylist grew from 12 to 19 entries, and a `'yes' in cmd` substring bug that could have confirmed a destructive action on a misheard "no, not yes I don't want that" was replaced with real word-boundary yes/no classification. `tests/unit/` now has 228 tests, `tests/integration/` wires real modules together (intent router → capability registry → real tools/system, faking only the genuinely dangerous/environment-dependent boundaries), and `.github/workflows/ci.yml` runs lint (`ruff`)/type-check (`mypy`)/the full suite on every push — so "no lint/type-check config, no CI" is no longer true. Standing up CI for the first time immediately caught four real, previously-invisible portability bugs (a hardcoded `E:\VORTEX` default that contradicted its own comment, `pygame.mixer.init()` crashing with no audio device, the wake model resolving against the wrong base path, openWakeWord's own preprocessing models never being explicitly provisioned) — all fixed, all verified against a real fresh GitHub Actions Windows runner, not just locally. No ADRs still. |
| 1 | Voice I/O Foundation | **Partial** | Mic capture, TTS playback, and chunked/interruptible speech work. **Offline STT/TTS fallback added 2026-08-16** (`src/vortex/voice/stt.py`, `voice/tts.py`): cloud stays the default/primary path — Google Web Speech (STT) and edge-tts (TTS) are tried first, exactly as before, unchanged behavior when online — and only fall back to a local model on a *network-reachability* failure specifically, not on "the service was reached but didn't like this audio/text": STT falls back on `sr.RequestError` only (never `sr.UnknownValueError` — that means Google understood the attempt fine, the audio just wasn't clear, and downgrading to a worse local model for that would be a downgrade, not a fallback); TTS falls back on `aiohttp.ClientConnectionError`/`asyncio.TimeoutError` only (never edge-tts's own `EdgeTTSException` family — that means the service *was* reached and returned something odd, a different, retriable problem). Offline STT uses `faster-whisper`'s `base.en` model (CPU, `int8`); offline TTS uses `piper-tts`'s `en_US-lessac-medium` voice — both gated by one kill switch, `VortexConfig.offline_fallback_enabled` (`VORTEX_OFFLINE_FALLBACK_ENABLED`, default on), and both degrade to a logged "unavailable" rather than crashing if the packages aren't installed or a model isn't cached yet (same probe-and-degrade contract as `documents.py`'s `_ocr_available`). Both engines lazy-load as an in-process singleton (loaded/downloaded at most once, not per call) and are explicitly warmed up (downloaded + loaded) from `main.py`'s existing `_warm_up_models()` hook — off the critical path, while the network is presumably still up — so the first real outage doesn't also have to be the first download attempt. **What's verified, and how:** (1) the exception-routing logic itself — `tests/unit/test_stt.py`/`test_tts.py`, 17 new tests, mocking `recognize_google`/`edge_tts.Communicate` to raise each exception type and asserting the offline path is/isn't called accordingly; full suite (`tests/unit/`) is 55/55 passing, the 38 pre-existing tests unchanged. (2) The offline engines for real, called directly through the actual `SpeechToText`/`TextToSpeech` classes (not mocks): a real edge-tts-synthesized clip ("turn off the living room lights") decoded to 16kHz PCM and run through `_recognize_offline()` came back `'turn off the living room lights.'` — correct. `_synth_offline()` on a real sentence produced a real ~185KB WAV, played for real through pygame (`[diag] offline _play took 4.19s`, in line with the sentence's natural length). **Not verified:** an actual simulated network outage inside the live app (`recognize_google`/edge-tts genuinely timing out end-to-end) — this dev environment doesn't have a clean way to sever outbound network for one process without affecting the sandbox itself, so the *trigger condition* is verified only via the mocked-exception unit tests above, not a real severed connection. **Real latency/quality trade-offs observed, not hidden:** offline is slower and less accurate than cloud, as expected for a fallback, not a replacement. STT: cold model construction in a fresh process ran 3.8-8.1s across repeated runs (real, measured, not smoothed over — CTranslate2 init cost varies run to run) before any real-time work is even absorbed by the startup warm-up; per-call inference once warm was sub-2s on a ~2s clip. `base.en` was chosen over `tiny.en` specifically because warm inference for the two was NOT meaningfully different (tiny.en 0.5-1.5s vs base.en 0.9-1.4s, measured back-to-back) — no latency reason to accept `tiny.en`'s known-worse accuracy. TTS: piper voice load ~4-11s cold (absorbed by warm-up), ~2-5s per-sentence synthesis once warm — both real (not synthetic-silence) generations were reviewed by ear via pygame playback and matched the input text. `faster-whisper`/`piper-tts` are a new optional extra, `voice-offline` (not bundled into `voice` or `all`) — split out deliberately for two verified reasons: real added install weight (`ctranslate2`+`av`+`piper-tts` wheels combined, before either engine's model weights are even downloaded) and `piper-tts` being GPL-3.0-or-later, a different license than the rest of this project's dependencies. No device-selection UI, no formal provider-interface abstraction (`speak`/`capture_command` are concrete methods with an offline branch inside them, not swappable adapters — a deliberate judgment call matching this project's existing "don't introduce an interface split before there's a second *implementation* driving the need for one" pattern, see both classes' docstrings). |
| 2 | Desktop & OS Automation | **Implemented (v1)** | **Bumped from Partial 2026-08-18**, on the same basis Phase 6 already uses "(v1)" for - the core acceptance scenario (open/close apps, bulk-close, confirmation gating before destructive actions) runs end-to-end reliably, repeatedly verified live this session: real Notepad opened/closed through the full pipeline three separate times across Steps 5/6/9/10, real close-all dispatch logic verified against a controlled fake process list (not the real machine's actual processes - that would be genuinely dangerous to test), and the exact flagged confirmation-bug scenario ("no, not yes I don't want that") confirmed declining correctly end-to-end. What's still missing (general permission/policy model beyond the fixed scope, audit-log rotation, filename-only search) are real, narrower refinements, not "does the core scenario work" gaps - same distinction Phase 6 already draws for its own known limitations. Open/close named apps, bulk-close with a protected-process allowlist, confirmation gating before shutdown/restart/close-all, lock-system (no confirmation needed - trivially reversible, loses no work). **File operations added (2026-08-16):** `src/vortex/files.py` - voice-triggered list ("list files", "list files in downloads"), search ("find file X", "search for files containing Y" - filename/glob matching only, not the content search Phase 5's RAG stack already does over document text), move, copy, and rename, plus delete via `send2trash` (Windows Recycle Bin, never a permanent `os.remove`). Scoped ONLY to the same three directories `documents.py` already resolves against (Desktop/Documents/Downloads, reusing `documents.SEARCH_DIRS` rather than a second, possibly-diverging path policy) - every resolved path is verified to actually live under one of those directories before being touched, so no `..`, absolute-path, or symlink voice input can reach anywhere else (`PathNotAllowedError` otherwise). Delete/move/rename are gated behind the same `self.awaiting_confirmation` yes/no flow already used for shutdown/restart/close-all; copy is not confirmation-gated - a deliberate, narrower judgment call (see `files.py`'s docstring) since it can't destroy or rename a pre-existing file, and move/copy/rename all now structurally refuse to overwrite an existing file at the destination (`FileExistsError`) rather than silently clobbering it. **Structured audit log added:** `src/vortex/audit.py` (`AuditLog`) writes one JSON object per line - timestamp, action, target, outcome, plus optional details - to a separate file (`logs/audit.jsonl` by default, `VORTEX_AUDIT_LOG`-overridable) for consequential actions: file delete/move, app close, shutdown/restart, and every confirmation prompt together with its eventual outcome (`prompted`/`executed`/`declined`/`failed`). This is additional to, not a replacement for, the existing plaintext `self.log()`/`logging.info()` calls, which are unchanged. **Capability registry added:** `execute()`'s regex-based if/elif chain was mechanically restructured into a list of `{name, matcher, handler, destructive, description}` entries it iterates in the same order as before - dispatch infrastructure only. It does **not** add authorization or policy: `destructive` is descriptive metadata, not an enforced gate (e.g. `close_named_app` - "close X" - is marked `destructive: True` but, exactly as before this change, still runs immediately with no confirmation prompt, unlike bulk-close). Every pre-existing voice command was verified, via `tests/unit/test_registry.py`, to still route to the exact same handler after the rewrite; 104 `pytest` tests pass, including 4 new files covering registry dispatch, file-op path-safety/no-overwrite/send2trash behavior, the audit log's JSON-lines format, and end-to-end confirmation gating for the new destructive commands. Still no general permission/policy model beyond the fixed three-directory scope and the fixed list of confirmation-gated actions, no audit-log rotation/retention, and search remains filename-only (not a content index). **Popup added (2026-08-17):** `list_files`/`search_files` now also open a small window (`src/vortex/popup.py`, tkinter on its own dedicated thread, never blocking the voice loop) listing each result as "filename (TYPE) - Location" at the same moment the spoken summary starts - direct user request for a synchronized visual + spoken listing instead of spoken-only. Verified directly: a real, visible window opens with the correct title, non-blocking. **Fixed bug:** "shutdown vortex" set internal flags to stop the worker/audio threads but never stopped the `pystray` tray icon, so `icon.run()` (blocking the main thread) never returned and the process lingered as a zombie - stopped listening/responding, but never actually exited. `stop()` now stops the icon too, unifying the voice-triggered shutdown and the tray's Exit item into one real shutdown path. |
| 3 | Browser Automation & Web Interaction | **Implemented (v1)** | **Bumped from Partial 2026-08-18** - navigate/search/click/read-page is this phase's own stated v1 scope ("honestly-scoped first slice... not yet form-filling, uploads, downloads" - see below), and that scope runs end-to-end, verified live this session with zero mocking: opened a real page (example.com) through real Playwright/Chromium, read the real page content back, ran a real DuckDuckGo search and got real results (Python.org, Wikipedia). Form-filling/uploads/downloads/multi-page auth were never in this phase's v1 scope to begin with, so their absence isn't a partial implementation of something attempted - it's the documented boundary of what v1 was ever going to cover. `src/vortex/browser.py`: Playwright-driven navigate/search/click/read-page/play-YouTube-video, voice-triggered ("go to...", "search for...", "click...", "read the page", "play X on youtube"). Search uses DuckDuckGo's plain-HTML endpoint, not Google — Google serves an automated browser a bot-detection page almost immediately, and working around that would mean bypassing an anti-abuse mechanism, which is out of bounds. All web-opening actions (including the old app-launcher's fallback) now route through this one controlled browser session instead of ever popping open the system's default browser. No form-filling, uploads/downloads, or multi-page authenticated workflows yet. Browser launches visibly (not headless) and lazily on first use. **Fixed bug:** "open youtube and play X" used to be swallowed whole by the generic app-launcher's `open (.+)` pattern, which — finding no app or exact web_app match — fell back to opening a literal Google search of the entire phrase in the system's separate default browser (not the automated one), producing search-results pages instead of playing anything. Now routed to an explicit YouTube search-and-click-first-result action before that fallback ever fires. |
| 4 | LLM Brain, Tool Calling & Hybrid Intent Routing | **Partial** | Regex-based deterministic router falls through to a streaming Ollama call with conversation history. **Structured tool-calling infrastructure added 2026-08-20** (`src/vortex/llm/tools.py`, `LLMProvider.chat_with_tools`/`OllamaProvider.chat_with_tools`, `Vortex._try_tool_call`) but shipped **off by default** (`VortexConfig.llm_tool_calling_enabled`, `VORTEX_LLM_TOOL_CALLING_ENABLED`, default `False`) on real, live-tested evidence it isn't reliable enough to act on yet - this is a deliberate not-yet-safe-to-enable decision, not an unfinished stub. Built: 4 safe, non-destructive tool schemas (`open_app`, `web_search`, `get_time`, `get_date` - deliberately excludes every destructive capability, since a hallucinated `open_app` call is a far cheaper mistake than a hallucinated file-delete or shutdown call), a defensive `tool_call_to_intent()` that fails closed (returns `None`, dispatches nothing) on any malformed/non-dict/wrong-type argument shape, and `execute()` only ever attempting a tool call when the deterministic regex router found nothing (`Unhandled`) - never overriding an already-matched intent. **Live-tested against every model on this machine, 2026-08-19:** `llama3.2:1b` (the only one that accepts Ollama's `tools` parameter at all) hallucinated a tool call for a plain unrelated general-knowledge question ("what is the capital of France" → called `get_date`), and on topically-right phrases echoed the parameter's own JSON schema back instead of the extracted value (rejected by `tool_call_to_intent`'s defensive checks, confirmed live: mapped to `None`, correctly fell through to the plain LLM answer). `llama3:latest` and `phi:latest` both reject the `tools` parameter outright (`does not support tools (status code: 400)`) - not a fallback path, a hard model-capability rejection. 19 new unit tests (`tests/unit/test_tools.py`: `tool_call_to_intent`'s valid-mapping/fail-closed cases, including the exact schema-echoed-back shape observed live; `tests/unit/test_registry.py`: `execute()`'s flag-off/flag-on/malformed-call/no-call/request-failure paths). No structured/JSON tool-call schema is acted on by default, no tool registry beyond this fixed 4-tool list, no prompt versioning or eval suite. |
| 5 | Memory, Knowledge & Production RAG | **Implemented (v1)** | **Bumped from Partial 2026-08-19**, same basis as Phases 2/3/6/7 - both named halves of this phase now have a working, live-verified core acceptance scenario. Two pieces, still not fully unified into one stack: (1) `src/vortex/memory.py` — conversation history persists across restarts in a local SQLite database instead of an in-RAM list. **Retrieval added 2026-08-19:** `rag.py`'s existing `RagStore` (same Postgres+Qdrant connections already open for document RAG, no second pair of services stood up) gained `index_turn`/`search_turns` - a second Qdrant collection (`vortex_conversation_turns`) indexes every turn as it's added, via a background daemon thread (`app.py`'s `_index_turn_async`) so a slow or failed embed/upsert never delays or breaks the live conversation; SQLite stays the sole durable, authoritative record regardless of whether indexing succeeds. "Do you remember X" / "what did I tell you about X" retrieves the most relevant past turns and answers via the LLM, same `awaiting_confirmation`-free pattern as document Q&A (read-only, no confirmation needed). Dense (embedding) search only, deliberately not the hybrid BM25+rerank pipeline document retrieval uses - conversation turns are short conversational text, not dense reference material where an exact keyword is as likely to matter as semantic similarity; can be revisited if real usage shows otherwise. Degrades to a clear spoken explanation, not a crash, if Postgres/Qdrant aren't running (`self.rag is None`). **Live end-to-end verified**, not just mocked: a real exchange ("my favorite programming language is Rust because of its safety guarantees") was indexed via a real background thread, then "do you remember my favorite programming language" correctly retrieved it and answered "Rust's safety guarantees make it a popular choice..." via a real Ollama call. 8 new unit tests (`test_memory_retrieval.py`) cover `index_turn`/`search_turns`/`build_memory_prompt` against a mocked Postgres/Qdrant; `test_registry.py` covers dispatch plus `recall_memory`'s three degrade-gracefully paths (`rag` unavailable, search failure, no relevant turns found). (Historical note: this retrieval addition was originally considered and deliberately scoped out on 2026-08-16, as a separate, involved piece of work not worth attempting hastily alongside that day's RAG hybrid-search changes - it landed properly three days later instead, once document RAG's infrastructure already existed to build on top of.) (2) `src/vortex/rag.py` — the real Postgres+Qdrant stack from the master blueprint, used specifically for document question-answering: documents are chunked, embedded locally (`nomic-embed-text` via Ollama), and indexed in Qdrant, with Postgres as the transactional source of truth for chunk text/metadata. A targeted question retrieves only the relevant chunks instead of truncating a long document. Runs against local, self-hosted Postgres and Qdrant (native Windows installs, not Docker — see README setup). Degrades gracefully to the old truncated-whole-document approach if Postgres/Qdrant aren't running. **Hybrid dense+sparse search and reranking added (2026-08-16):** `retrieve()` now pulls a candidate pool from Qdrant's dense cosine search *and* BM25 keyword search (`rank_bm25`, pure-Python, only depends on numpy already pinned for the wake-word stack) over the same document's full chunk text, combines them with Reciprocal Rank Fusion (rank-only combination — chosen specifically because cosine similarity and BM25 scores live on incomparable scales, so a weighted blend would need ad-hoc normalization RRF avoids entirely), then reranks the fused top-10 with a keyword-overlap/exact-substring heuristic before truncating to the final top-k. **Deliberately not a cross-encoder reranker:** checked `sentence-transformers`' actual install footprint before deciding (`pip install sentence-transformers --dry-run`) — it resolves a 122MB torch wheel (Windows, cp311) plus transformers/tokenizers/safetensors, and a real reranker model would be a *further* separate download at first use. That's a real mismatch for an otherwise fully local, lightweight app, so a cheap keyword-overlap heuristic was used instead — see `rag.py`'s module docstring for the full reasoning. Both stages are independently off-switchable (`VORTEX_RAG_HYBRID_SEARCH`, `VORTEX_RAG_RERANK`, new `VortexConfig` fields `rag_hybrid_search`/`rag_rerank`, both default on). **Real trade-off, not free:** hybrid mode adds one Postgres query per uncached document (fetching the BM25 corpus, then cached in memory) plus BM25/RRF/rerank CPU work on top of the existing embed-and-search round trip — not measured against a hard latency budget, but it is strictly more work per question than the previous dense-only path. **Verified:** `retrieve()`'s signature and return shape are unchanged, so `main.py`'s one call site needed zero edits. 22 new unit tests (`tests/unit/test_rag_hybrid.py`) cover BM25 scoring, RRF fusion, the keyword-overlap reranker, and `RagStore.retrieve()`'s wiring against mocked Postgres/Qdrant — including a synthetic (explicitly simulated, not live) case showing a dense-only top-3 that misses an exact-keyword chunk get recovered into the hybrid top-3 by fusion. **Live end-to-end verified** against real Postgres, Qdrant (started temporarily via its scheduled task, stopped again after), and Ollama (`nomic-embed-text`): ingested a synthetic multi-chunk facility-manual document containing several near-identical "rotating access code" paragraphs (deliberately adversarial — same sentence template, different code) plus a paraphrasable fact; the full pipeline (dense search → BM25 → RRF fusion → rerank) ran correctly end-to-end and returned the right chunk. Honest finding from that live run: `nomic-embed-text`'s dense-only search *also* ranked the correct exact-code chunk first in every live trial run here — evidently strong enough at literal substring/subword overlap that it didn't need rescuing by BM25 in this specific case, so the dramatic dense-miss-hybrid-catches scenario was only demonstrated in the synthetic unit test, not forced live within this session's testing budget; hybrid search did not regress anything either (paraphrase query for the semantic fact still worked via the dense signal). **Incidental bug found and fixed while testing this:** `ensure_ingested()` re-ingesting a changed document deleted the old Postgres chunk rows but never told Qdrant to drop the matching points, so a document's *previous* version's chunk vectors stayed indexed forever, silently polluting future dense search for that document — `ensure_ingested()` now calls `qdrant.delete()` scoped to that `document_id` before upserting the new points (best-effort: a failed delete doesn't block re-ingestion, just leaves stale points for next time), covered by 2 new unit tests. |
| 6 | Wake Word, Session Mode & Barge-In | **Implemented (v1)** | Furthest-along phase. Custom-trained "Hey Vortex" ONNX wake model (own training pipeline, not Porcupine), automatic gain control on the wake audio path, mid-speech barge-in, and multi-turn active session with inactivity timeout so you don't repeat the wake word for follow-ups or confirmations. Not yet measured against an explicit CPU/RAM budget; false-accept/reject rates are calibrated against synthetic TTS voices only, not a large real-microphone test set. **Barge-in confirmed working (2026-08-16):** verified end-to-end with a real acoustic test - synthesized audio played through the actual speakers, picked up by the actual mic, not a mock or unit test. Sequence: wake → "Yes Boss?" → question captured and answered → "Hey Vortex" said again mid-response → interrupted mid-sentence (score 0.774 against a 0.75 threshold) → confirmed silent within ~1.5s. The earlier reports of barge-in not working were most likely the wake-stream-death bug above, not the threshold itself - once the stream stopped dying, barge-in worked on the first live test. **Known open issue:** false wake activations still occur in real-world conditions during standby (reduced, not eliminated, by the AGC noise-floor fix). Diagnostic logging (score + noise floor per trigger, in `_on_audio`) is kept permanently, not temporarily, specifically to make the next occurrence tunable from real data. **Fixed bug:** barge-in cut off speech with total silence and no acknowledgment - only a fresh standby wake spoke "Yes Boss?"; a barge-in went straight to listening with no audible signal the interruption had registered. Both paths now speak "Yes Boss?" for a consistent, unambiguous confirmation. **Fixed bug (2026-08-16):** the wake word could silently stop registering entirely after the app had run a while, with no error logged anywhere - caused by reusing one `InputStream` across hundreds of stop/start cycles (one pair per command), which eventually stopped delivering audio to the callback. Fixed by recreating the stream on every mic handoff instead of reusing it, plus a watchdog that rebuilds the stream if no audio callback fires for 5+ seconds while idle. See `CHANGELOG.md`. **Diagnosed (2026-08-16):** a separate, real barge-in failure mode on laptops - the mic and speakers sit close together, so VORTEX's own TTS output dominates the mic signal while it's talking, and `_agc` cannot separate two overlapping voices in one channel (that needs true acoustic echo cancellation, not implemented). Confirmed via live logs: zero wake-model scores above 0.3 during a 25-second window while the user was actively trying to interrupt. Mitigated, not fixed, by turning VORTEX's own output down while speaking (`VORTEX_TTS_VOLUME`, default `0.6`). This does not make generic words like "wait" trigger an interrupt -  **Threshold now 0.60 (2026-08-17):** lowered again from 0.65 after direct evidence it was still too tight - the exact wake model + AGC pipeline, run in isolation on a real captured utterance, scored a max of 0.6499, just under the old bar. `VORTEX_TTS_VOLUME` default is `1.0` (full), not `0.6` - the 0.6 mitigation mentioned above made VORTEX inaudible in real use and was reverted the same day; see CHANGELOG.md 2026-08-16. **Honest open finding (2026-08-17):** synthetic acoustic re-testing after the 0.60 change still frequently failed to register any wake score at all, despite verifying raw audio reaching the mic was strong and the wake model scores this audio correctly in isolation - not yet root-caused, may be specific to a long-running process interacting with repeated synthetic test playback rather than a real regression (a real human voice attempt during the same session did score correctly). |
| 7 | Document Intelligence | **Implemented (v1)** | **Bumped from Partial 2026-08-18**, same basis as Phases 2/3/6 - the core acceptance scenario (find a document, summarize it, answer a targeted question about it) runs end-to-end, verified live this session with a real Ollama call: real file read, real summary produced from real content, and real RAG-backed retrieval that correctly pulled the one relevant section out of three near-identical candidates and answered from it, not the other two. OCR (a sub-feature for scanned PDFs specifically, not the primary text-extraction path) remains the one known, honestly-documented gap - Tesseract genuinely isn't installed on this dev machine, so only the graceful-degradation path has been verified for real, not OCR success itself. That's the same category of known-but-real limitation Phase 6 already carries ("false wake activations still occur") without it blocking "(v1)" - a documented gap in a secondary path, not a failure of the core scenario. `src/vortex/documents.py`: reads PDF (PyMuPDF)/DOCX (python-docx)/XLSX (openpyxl)/text files by voice ("summarize...", "read...", "what does X say about Y"), resolving filenames against Desktop/Documents/Downloads. Targeted questions ("what does X say about Y") now use the Phase 5 RAG stack (chunked + embedded + retrieved) instead of truncation. Plain "summarize" still truncates the whole document (summarization wants the whole document, not similarity-retrieved snippets — a genuinely different problem, not solved by retrieval). **OCR fallback added (2026-08-16):** a PDF page whose native PyMuPDF text is under ~20 characters (the signal a page is a scanned image with no text layer, not a false-positive-proof one) is rasterized via `page.get_pixmap()` and run through `pytesseract` if the Tesseract OCR binary is present. **Not verified against a real scanned PDF** — the Tesseract binary itself is not installed on this dev machine (`where tesseract` finds nothing; `pytesseract` 0.3.13, the Python wrapper, is installed and declared in `pyproject.toml`'s `documents` extra, but that alone does not make OCR functional). What IS verified live: the near-empty-text detection correctly identifies candidate pages, and the code degrades gracefully — logs one clear warning, skips OCR, keeps whatever native text existed — rather than crashing or silently claiming OCR ran. Manual install step needed for OCR to actually do anything: install Tesseract (e.g. https://github.com/UB-Mannheim/tesseract/wiki on Windows) and put `tesseract.exe` on PATH, then restart VORTEX. **Page/section provenance added (2026-08-16):** `extract_pages()` (new, alongside the existing flat `extract_text()`) returns page-tagged text for PDFs (1-indexed), heading-grouped sections for DOCX, and per-sheet sections for XLSX. `rag.py`'s chunker now chunks per page/section instead of one flattened string, and stores `page`/`section` alongside each chunk in both Postgres (`chunks.page`, `chunks.section` — added via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, verified against the live local Postgres instance) and Qdrant's payload. `build_rag_prompt()` labels each retrieved excerpt (`[Page 3]` / `[Section Name]`) and instructs the model to cite it. **Verified end-to-end live** (Postgres and Ollama were already running; Qdrant was started temporarily for this test, since it isn't running by default on this machine, then stopped again afterward): ingested a synthetic 3-page PDF, asked "What was Q3 revenue?", got back real retrieved chunks correctly tagged `page: 2`/`page: 3`/`page: 1`, and a real `llama3.2:1b` answer that said *"On page 2, revenue was 5.2 million dollars."* — provenance actually reaching the spoken answer, not just logged and discarded. Both additions are gated by new `VortexConfig` fields (`ocr_enabled`, `ocr_language`, `document_page_numbers`), off-switchable via `VORTEX_OCR_ENABLED` / `VORTEX_DOCUMENT_PAGE_NUMBERS`. |
| 15 | Security, Identity & Policy Enforcement | **Partial** | Protected-process allowlist and explicit yes/no confirmation before destructive OS actions exist. No OAuth/OIDC, no secrets vault (only `.env` + `dotenv`), no dependency/container scanning, no sandboxing (there's no generated-code execution yet to sandbox). |
| 8 | Vision | **Started** | `src/vortex/screen.py` (2026-08-17, direct user request): "read my screen" screenshots the current screen (`PIL.ImageGrab`) and OCRs it via the same Tesseract dependency `documents.py` already uses for scanned PDFs, speaking the extracted text back. Screen *capture* is verified working for real (a genuine screenshot, correct resolution). Actually reading text needs the Tesseract binary installed, which isn't set up on this dev machine - degrades to a clear spoken explanation rather than a crash or a false claim of success, same honest contract as document OCR. This is OCR-based text reading only - no image/layout/UI-element understanding (no vision-capable model is installed; none of the Ollama models on this machine support vision), no continuous/live screen monitoring, voice-triggered on demand only. |
| N/A | Communication (Email) | **Started** | Not one of the original 17 numbered phases - added 2026-08-18 on direct user request ("auto-reply for messages/mail/calls, only with my approval"), scoped down to a buildable-today first slice: email only, voice-triggered only, not proactive/background. `src/vortex/mail.py`: Gmail API integration (`gmail.readonly` + `gmail.send` scopes, not the broader `gmail.modify` - VORTEX never deletes/archives/labels anything), lazy-constructed exactly like `browser.py`'s `BrowserAgent` (zero network/OAuth calls until first real use, so `Vortex()` construction stays exactly as safe as before for every existing test). "Check my email" summarizes unread messages; "reply to X and say Y" resolves X against unread senders/subjects, drafts a reply via the LLM (`Vortex.draft_email_reply`), and speaks it back for an explicit yes/no - sending only happens from `handle_confirmation()` on "yes", the identical `awaiting_confirmation` gate every other destructive action (file delete/move/rename, shutdown/restart/close-all) already uses. 19 new unit tests: `test_mail.py` (11) mocks the Gmail API client directly (MIME parsing, reply-threading headers, Re: prefix handling) without ever running OAuth; `test_registry.py` (8) exercises the full `execute()` pipeline including the exact ambiguous-match and not-configured-yet degrade paths. **Not yet live-verified against a real Gmail account** - that needs a one-time OAuth consent in a real browser after Google Cloud Console setup (see README's "Setting up Gmail"), which only the account owner can do; verified so far via the mocked test suite and via the real degrade-gracefully path (no `credentials.json` present) through the actual `execute()` pipeline, not via a real sent email. No proactive/background mail checking (deliberately deferred - would need a polling worker integrated with the wake/session state machine, a separate and larger piece of work), no messaging (SMS/WhatsApp) or phone-call handling yet (both need external provider accounts - Twilio etc - the user would need to set up first). |
| N/A | Standby/Activation/Personality/Owner-Context Foundation | **Started** | Not one of the original 17 numbered phases - added 2026-09-01 on direct user request, explicitly scoped as *foundation* work for a future Jarvis/Friday-level personality and multi-model/multi-agent layer, not that layer itself. Two real architectural conflicts were found and resolved with the user before writing any code (see this feature's design doc / commit message for the full reasoning): (1) the spec's "Shutdown Vortex" (silent, wake-word-reactivatable) would have collided with the existing, tested `"shutdown vortex"` voice command (full process exit, unchanged) - resolved by adding a distinct new phrase, `"stand down"`, instead of repurposing the existing one; (2) `PROCESSING`+`EXECUTING` as two separate states from the spec's minimum list were collapsed into one real `EXECUTING` state (`core/state_manager.py`) - intent classification is a sub-millisecond regex match in this codebase, not an observably separate phase from dispatch, so a second state with no real signal behind it would have been exactly the kind of unearned/fake distinction this project's own conventions argue against. **What's real and live-verified:** `"stand down"` (new `StandDown` intent) ends the current active session immediately and silently via a new `Session.end_session_now` Event, mirroring exactly how `in_active_session` was added in Step 7 (purely additive, untouched barge-in-critical timing) - verified via a real threaded-worker test (`test_lifecycle.py`) that a wake still works again afterward, unlike a full "shutdown vortex" exit. `activation_response`/`barge_in_response` (previously hardcoded `'Yes Boss?'`/`"Yes Boss, I'm listening."` literals in `voice/session.py`) are now `VortexConfig` fields. `core/personality.py`'s `PersonalityMode` enum (PROFESSIONAL/FRIENDLY/WITTY/PROTECTIVE/DEMO) is runtime-switchable via a new voice command ("switch to X mode") and has exactly one live integration point: `app.py`'s `ask_llm_stream()` now builds its system prompt through `personality.build_system_prompt()` instead of a flat constant - **live-verified against real Ollama**, same question asked in PROFESSIONAL vs. WITTY mode produced two different real answers, confirming the wiring genuinely reaches the model (honest caveat: with `llama3.2:1b`, the tonal difference was real but subtle, not a dramatic personality shift - consistent with this model's other documented limitations this session). PROTECTIVE's directive includes, as literal prompt text (not just a comment), the feature's one non-negotiable principle: VORTEX must never blindly defend the owner when the owner is factually wrong. `core/owner_context.py`'s `OwnerContext` is a thin, single-owner identity object (`owner_id`/`display_name`/`preferred_address`, the latter two reusing the existing `config.user_name` rather than a duplicate knob) whose `session_state`/`personality_mode` are live-delegating properties, not copied fields - avoiding the exact "stale snapshot" problem `state_manager.py` already documents for `VortexState`. `core/social_context.py`'s `classify()` is a deliberately simple, conservative, rule-based (keyword/phrase-shape) classifier - NOT sentiment analysis or a trained model - defaulting to `AMBIGUOUS`/`NORMAL` whenever unsure, same fail-closed philosophy as `llm/tools.py`'s `tool_call_to_intent()`; its output feeds an additional directive into `build_system_prompt()`. **Honesty note on accuracy:** "100% accuracy" isn't a meaningful claim for a rule-based classifier with no ground truth - what IS fully tested and guaranteed is structural, not statistical: no personality mode x social label combination this code can produce ever yields an aggressive, insulting, or threatening directive (verified across all 30 combinations in `test_personality.py`). `presentation_mode` is a derived read-only property (`personality_mode == DEMO`), not a second independently-settable flag, avoiding two toggles that could drift out of sync for no real benefit at this stage - no current code path leaks internal diagnostics into spoken output today (logs only ever reach `vortex.log`), so this ships as a real, tested, queryable extension point rather than a fix to a currently-leaking problem that doesn't exist yet. **Fixed same day, found during re-testing:** the literal phrase from the feature spec itself, "VORTEX, demonstrate yourself," did not originally route to anything - only the more mechanical "switch to demo mode" phrasing worked, so the natural request the whole feature is named after silently fell through to the plain LLM. Added a dedicated `_DEMO_TRIGGER` pattern (`"demonstrate yourself"`, `"show me what you can do"`, `"give me a demo(nstration)"`) mapping to the same `SetPersonalityMode(mode='demo')` intent. **Live-verified end to end**, including a real Ollama call: after "demonstrate yourself," a real question about internal tools/architecture got back *"I don't have access to any proprietary or sensitive internal tools or data, and I am not permitted to disclose any information about my underlying infrastructure or architecture"* - the DEMO directive actually shaping a real answer, unprompted, not just present in the system prompt on paper. **Real self-demonstration content added same day, on direct user request** ("test it properly - can it explain what it can do, how it was built, the journey with me, future plans, current drawbacks"): entering DEMO mode alone only changed the *tone* of future answers, it never actually said any of that. `Vortex.demonstrate_self()` (`app.py`) now speaks it directly. **First attempt routed this through the LLM** (a grounding prompt with all the real facts, asking the model to narrate them) - live-tested and rejected: even with a raised token budget, `llama3.2:1b` only recited a few of ~29 raw capability strings near the start of the prompt, silently dropped the build-history/relationship/plans/drawbacks sections entirely, and closed with "Would you like to proceed with any of these actions?" - misreading a self-introduction as an action menu. Same class of failure already documented for this model elsewhere (tool-calling hallucinations) - synthesizing several distinct facts into one coherent answer is unreliable for a 1B-parameter model, and completeness matters more than LLM variety for a request that explicitly needs to "explain all." **Redesigned as fully deterministic** (`core/self_knowledge.py`, no LLM call at all): a curated, hand-written capabilities overview (cross-checked against `intent_router.ALL_INTENT_TYPES` via a test, so it can't silently drift from what VORTEX actually routes), a short build-history paragraph, real conversation-history stats from a new `MemoryStore.stats()` method (`turn_count`/`first_turn_at`, actually queried, not invented), the same honest future-plans/drawbacks content, all spoken as one complete paragraph. **Live-verified, real output**: *"...We've exchanged 490 messages together since 2026-08-02 11:36:25..."* - a real number from real conversation history, followed by real future plans and honest drawbacks, complete and never truncated. 7 new unit tests (`test_self_knowledge.py`) plus 3 for `MemoryStore.stats()` (`test_memory.py`) and updated dispatch tests (`test_registry.py`, `test_lifecycle.py`). **Real barge-in failure found and fixed, same day, on direct user report** ("barge-in fails during the demonstration"): live log evidence was unambiguous - the wake detector logged zero diagnostic scores, not even a low failed attempt, for the entire ~107 seconds of the (then-unbroken) self-introduction, while a wake attempt made the instant VORTEX fell silent afterward succeeded within milliseconds (score 0.752, immediately triggered). This is this project's already-documented near-field self-noise limitation (VORTEX's own voice masking the mic - see `tts_volume`'s docstring in `config.py`), but no feature had ever produced continuous speech this long in one unbroken block before, so nothing had stress-tested it this severely. **Fix:** `core/self_knowledge.py`'s `build_demo_segments()` now returns the five topics as a list instead of one joined string; `demonstrate_self()` speaks each as its own utterance with a real, configurable pause between them (`VortexConfig.demo_segment_pause`, `VORTEX_DEMO_SEGMENT_PAUSE`, default 0.6s - a judgment call, not independently re-derived through repeated trials the way `wake_threshold` was), and checks `stop_speaking` between segments so a barge-in registered after one topic stops the rest of the introduction instead of continuing regardless. Honest scope: this doesn't fix the underlying acoustic problem (true acoustic echo cancellation still isn't implemented) - it gives the wake model several genuine quiet windows across the introduction instead of zero. Live-verified the pauses are real (measured ~0.59-0.61s gaps between segment calls); verifying the *acoustic* improvement itself needs the user's real microphone, not something checkable from this environment. 5 new/updated unit tests. **Still failed on real re-test - fixed properly, same day:** the first fix above only paused *between* the five broad topics, and `CAPABILITIES_SUMMARY` alone is six sentences (~25-30s of continuous speech with zero internal breaks) - exactly the block a user naturally tries to interrupt first. Real log evidence confirmed it: six "Speaking:" lines fired back-to-back with no real gap, all from inside that single topic-level segment. **Real fix:** `build_demo_segments()` now splits every constant down to individual SENTENCES (a small regex, `_sentences()`) instead of five topic blocks, so the pause lands after literally every sentence spoken (13 sentences total, not 5) - live-verified the real segment count and ~0.59-0.61s gaps after every one, not just between topics. Same honest caveat as before: doesn't fix the underlying acoustic problem, gives many more genuine quiet windows than either previous version. 2 more updated unit tests. **Two more real bugs found and fixed, same day, from direct user re-testing:** (1) a follow-up question referencing VORTEX's own PREVIOUS answer ("can you give a demonstration on that") matched a too-loose `_DEMO_TRIGGER` bare pattern ("give a demo(nstration)" with no object) and hijacked into the full 13-sentence self-introduction, completely ignoring what "that" referred to - fixed by requiring an explicit "of yourself"/"of what you can do" object on that branch, so a bare "demonstrate"/"give a demonstration" with no object now correctly falls through to the LLM as an ordinary follow-up instead of being assumed to mean "introduce yourself again." (2) Asked "what makes you different from other assistants" with no dedicated handling, the plain LLM fallback fabricated *"I possess a unique ability to understand and respond to subtle emotional cues"* - a capability VORTEX doesn't have, a real hallucination, not just a weak answer. New `WhatMakesYouDifferent` intent routes this to `core/self_knowledge.py`'s new `DIFFERENTIATION_SUMMARY` instead - spoken directly, no LLM call, every claim independently checkable against the actual codebase (local Ollama model, real tested code with public history, documentation that says "Partial" instead of overclaiming) rather than generic assistant-marketing language. Live-verified end to end: the real answer now given, and the follow-up phrase confirmed to no longer touch `personality_mode`. 6 new/updated unit tests. Phase 7's "future-proof interfaces" ask is honored as documentation only (each new module's docstring notes its extension seam for a future multi-model router) - no new agent/orchestrator classes were added, per the spec's own explicit instruction not to build that yet. 71 new unit tests total across `test_personality.py`, `test_owner_context.py`, `test_social_context.py`, `test_self_knowledge.py`, `test_memory.py`, `test_lifecycle.py`, plus extensions to `test_state_manager.py`, `test_intent_routing.py`, `test_registry.py`, and `test_config.py` - full suite at 350 passed (1 hardware test deselected), zero regressions. |

## Not started (no code yet)

Phases 9 (FastAPI/Event-Driven Core), 10 (Durable Workflows), 11
(Career Intelligence), 12 (Job Discovery/Application Automation), 13
(Multi-Agent Orchestration), 14 (Developer Agent), and 16 (Observability/
Deployment) have no code in this repo. Each will get its own row here, its
own README update, and its own section in the learning guide when it
actually lands.

## A capability the blueprint didn't spell out in detail, but is real today

**Self-trained wake-word model** (`tools/wakeword/`): a from-scratch pipeline
that synthesizes training audio (edge-tts across ~20 voices), extracts
embeddings via openWakeWord's shared preprocessor, trains a small MLP
classifier, runs a hard-negative-mining pass over long synthetic audio
streams, and exports the result as a standalone ONNX model matching
openWakeWord's exact I/O contract. This is a narrow, supervised,
single-classifier training loop — not general model training — but it's a
concrete first instance of "VORTEX can train its own models."

## Honest gaps versus the blueprint's own Phase 6 requirements

- *"Wake detection must not require network access"* — true for wake
  detection itself (fully local ONNX). STT/TTS now have an offline fallback
  (`faster-whisper`/`piper-tts`, see Phase 1's row above) that engages
  specifically on a network-reachability failure, but the fallback's trigger
  condition is verified only via mocked exceptions in unit tests, not a real
  severed network connection against the live app end-to-end — so "a network
  outage silences VORTEX" should be read as "should no longer, pending a real
  live outage test," not as a fully closed gap yet.
- *CPU/RAM budget on 8GB-class hardware* — never measured. Should be profiled
  before calling Phase 6 done.
- *Barge-in with real human voice, not just the trained wake phrase* — only
  "Hey Vortex" is ever checked as an interrupt trigger; generic words like
  "wait" or "stop" do nothing, by design (a VAD-based "any speech interrupts"
  approach would make VORTEX constantly interrupt itself, since its own TTS
  output is speech too). Near-field self-noise (mic hears VORTEX louder than
  the user while it's talking) is mitigated via `VORTEX_TTS_VOLUME` but not
  solved - true acoustic echo cancellation would be required to solve it
  fully, and isn't implemented.
