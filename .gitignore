# Secrets - never commit
.env

# Virtual environment
venv/
.venv/

# Python
__pycache__/
*.pyc
*.pyo

# pip install -e . / pytest build artifacts
*.egg-info/
.pytest_cache/

# Runtime logs
logs/*.log

# Persistent conversation memory - contains real conversation transcripts
data/

# Regeneratable synthetic training audio (34MB+, rebuilt by build_hey_vortex.py)
tools/wakeword/cache/

# Qdrant binary (84MB, downloaded by the setup steps in README) and its local
# vector storage (contains embedded content from your own documents)
tools/qdrant/qdrant.exe
tools/qdrant/storage/
tools/qdrant/snapshots/
tools/qdrant/.qdrant-initialized

# Empty placeholder directory, not part of the source tree
/models/

# Custom voice training (tools/voice_training/) - real recorded voice audio,
# training checkpoints, and exported models are personal data / large
# binaries, never committed. See tools/voice_training/README.md.
tools/voice_training/dataset/
tools/voice_training/smoke_test_dataset/
tools/voice_training/runs/
tools/voice_training/exported/
tools/voice_training/lightning_logs/
tools/voice_training/checkpoints/
